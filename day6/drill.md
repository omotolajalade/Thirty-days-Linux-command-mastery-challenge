
Create a script file and set it to rwxr-xr-x using the three different chmod methods: relative, assignment, and octal. Confirm the result with ls -l after each change.
Create a file called script.sh.
Enter the command line

Check the file's current permissions using ls -l.

Change the permissions using the relative method with chmod.

Check the permissions again using ls -l.

Change the permissions using the assignment method with chmod.

Check the permissions again usinf ls -l

Change the permissions using the octal method with chmod 755.

Check the permissions using ls -l.
